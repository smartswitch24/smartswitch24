---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import Header from '../../../components/Header.astro';
import Footer from '../../../components/Footer.astro';
import FinanceDisclaimer from '../../../components/FinanceDisclaimer.astro';
import TrustBar from '../../../components/TrustBar.astro';
import FaqList from '../../../components/FaqList.astro';
import CalculatorEmbed from '../../../components/CalculatorEmbed.astro';
import { de as t } from '../../../i18n/de';
import { tagesgeldWidget } from '../../../config/affiliate-widgets';

const lang = 'de';
const siteUrl = 'https://smartswitch24.de';
const canonical = `${siteUrl}/de/tagesgeld/`;
const alternateUrl = `${siteUrl}/ar/tagesgeld/`;
const faqs = [
  { q: t.tagesgeld_faq1_q, a: t.tagesgeld_faq1_a },
  { q: t.tagesgeld_faq2_q, a: t.tagesgeld_faq2_a },
];
---
<BaseLayout title={t.tagesgeld_meta_title} description={t.tagesgeld_meta_description} lang={lang} canonical={canonical} alternateUrl={alternateUrl}>
  <Header lang={lang} activePage="finanzen" />
  <FinanceDisclaimer lang={lang} />
  <div class="page-header">
    <div class="container">
      <h1>{t.tagesgeld_page_title}</h1>
      <p>{t.tagesgeld_page_subtitle}</p>
    </div>
  </div>
  <CalculatorEmbed lang={lang} title={tagesgeldWidget.titleDe} description={tagesgeldWidget.descriptionDe} providerName={tagesgeldWidget.providerName} htmlCode={tagesgeldWidget.htmlCode} disclaimer={tagesgeldWidget.disclaimerDe} />
  <section>
    <div class="container">
      <FaqList title={t.tagesgeld_faq_title} items={faqs} />
      <p style="margin-top:24px;"><a href="/de/finanzen/" style="color:var(--color-primary);">← Zurück zu Finanzen</a></p>
    </div>
  </section>
  <TrustBar lang={lang} />
  <Footer lang={lang} />
</BaseLayout>
